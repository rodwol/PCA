from setuptools import setup, find_packages

setup(
    name='alumathp7',
    version='0.1.0',
    packages=find_packages(),
    install_requires=['numpy'],
    description='Matrix multiplication for different dimension matrices',
    author='Peer Group 7',
    license='MIT'
)

